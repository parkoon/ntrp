import styled from 'styled-components'

export const Wrapper = styled.div`
  margin-bottom: 32px;
`
export const Title = styled.h2`
  font-size: 32px;
  font-weight: bold;

  word-break: keep-all;

  line-height: 42px;

  width: 520px;
`
