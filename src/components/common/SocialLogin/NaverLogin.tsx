import React from 'react'
import styled from 'styled-components'

import { NaverIcon } from '../Icons'

function NaverLogin() {
  return (
    <Wrapper>
      <NaverIcon />
    </Wrapper>
  )
}

const Wrapper = styled.a``

export default NaverLogin
